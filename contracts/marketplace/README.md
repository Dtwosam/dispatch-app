# Legacy Contract Scaffold

This directory is an earlier scaffold and is no longer the active contract source of truth.

## Active contract package

Use:

- `packages/contracts/marketplace/agent_registry.py`
- `packages/contracts/marketplace/task_escrow.py`

## Why this folder still exists

- it preserves the earlier single-file scaffold used during early exploration
- it may still be useful as a reference when comparing packaging approaches for Studio

## MVP truth

The production-facing MVP contract layer now lives under `packages/contracts`.
